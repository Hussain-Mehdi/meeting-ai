fastapi>=0.110,<1
uvicorn[standard]>=0.27,<1
pydantic>=2.6,<3
pydantic-settings>=2.2,<3
python-dotenv>=1,<2
httpx>=0.27,<1
faster-whisper>=1.0,<2
# Apple Silicon: Whisper on Metal (same large-v3 weights, many times faster than CPU)
mlx-whisper>=0.4,<1; sys_platform == 'darwin' and platform_machine == 'arm64'
# Local speaker embeddings for diarization (no account or token needed)
sherpa-onnx>=1.10,<2
scipy>=1.11,<2
python-multipart>=0.0.9,<1
arabic-reshaper>=3,<4
python-bidi>=0.6,<1
reportlab>=4,<5
pytest>=8,<9
